name: Lint
on: [push]

jobs:
  run-lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        run:  ls ${{ github.workspace }}
      - run: npm run lint:check
