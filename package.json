{
  "name": "d1-tutorial",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "d1:query": " npx wrangler d1 execute prod-d1-tutorial --local --command='SELECT * FROM Users'",
    "d1:init": "npx wrangler d1 execute prod-d1-tutorial --local --file=./drizzle/0001_young_randall.sql",
    "db:generate": "drizzle-kit generate:sqlite --schema=src/schema.ts",
    "test": "jest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240222.0",
    "@types/jest": "^29.5.12",
    "drizzle-kit": "^0.20.14",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.0.4",
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "drizzle-orm": "^0.29.4"
  }
}
